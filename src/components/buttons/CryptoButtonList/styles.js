import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    cryptoButtonList: {
        display: 'flex',
        flexDirection: 'row',
        listStyleType: 'none',
    },
});

export { useStyles };
